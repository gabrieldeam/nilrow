export interface SubHeaderProps {
    title: string;
    handleBack?: () => void;
    handleDelete?: () => void;
    showDeleteButton?: boolean;
    showOrdersButton?: boolean;
    handleOrders?: () => void;
  }
  